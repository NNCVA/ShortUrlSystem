package com.example.shorturl.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS 跨域配置已移至 WebConfig.java
 * 此文件保留以避免其他地方引用报错
 */
@Configuration
public class CorsConfig {
    // CORS 过滤器现在在 WebConfig.java 中配置
}
