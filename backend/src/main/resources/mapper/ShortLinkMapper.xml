<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shorturl.mapper.ShortLinkMapper">

    <resultMap id="BaseResultMap" type="com.example.shorturl.entity.ShortLink">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="original_url" property="originalUrl"/>
        <result column="short_code" property="shortCode"/>
        <result column="status" property="status"/>
        <result column="click_count" property="clickCount"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, name, original_url, short_code, status, click_count, created_at, updated_at
        FROM short_links
        WHERE id = #{id}
    </select>

    <select id="selectByShortCode" resultMap="BaseResultMap">
        SELECT id, name, original_url, short_code, status, click_count, created_at, updated_at
        FROM short_links
        WHERE short_code = #{shortCode}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, name, original_url, short_code, status, click_count, created_at, updated_at
        FROM short_links
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR original_url LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM short_links
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR original_url LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.example.shorturl.entity.ShortLink" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO short_links (name, original_url, short_code, status, click_count)
        VALUES (#{name}, #{originalUrl}, #{shortCode}, #{status}, #{clickCount})
    </insert>

    <update id="updateById" parameterType="com.example.shorturl.entity.ShortLink">
        UPDATE short_links
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="originalUrl != null">original_url = #{originalUrl},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM short_links WHERE id = #{id}
    </delete>

    <update id="incrementClickCount">
        UPDATE short_links
        SET click_count = click_count + 1
        WHERE id = #{id}
    </update>

</mapper>
